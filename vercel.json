{
  "buildCommand": "cd Veilpay && npm run build",
  "outputDirectory": "Veilpay/dist",
  "installCommand": "cd Veilpay && npm install",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
